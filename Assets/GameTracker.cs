using UnityEngine;

public class GameTracker : MonoBehaviour
{
    public static bool OrderPicked = false;

    public static bool TruckLoaded = false;

    // Start is called before the first frame update
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }
}